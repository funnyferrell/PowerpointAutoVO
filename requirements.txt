# Core dependencies
python-pptx>=0.6.21
requests>=2.28.0
pywin32>=306

# Kokoro TTS (local, free)
kokoro-onnx>=0.4.0
soundfile>=0.12.0

# Note: Also requires FFmpeg in PATH
# Install with: winget install ffmpeg
# Or download from: https://ffmpeg.org/download.html
